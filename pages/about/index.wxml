<view class="container">
    <view class="group" wx:for="{{list}}" bindtap="toTargetPage" data-target="{{item.target}}">
        <text>{{item.name}}</text>
        <view>
            <image src="../../images/my/arrow.png" class="arrow"></image>
        </view>
    </view>
</view>