var dateSubstring = function (val) { //截取后台返回的时间字段
  console.log(val)
  if (val.length == 0 || val == undefined) {
    return;
  }
  console.log(val)
  return val.substring(0, 10);
}
module.exports.dateSubstring = dateSubstring;
